<%= form_for @comment, :url => job_comments_path(@job.id) do |f|  %>
  <div class="field">
    <%= f.text_area :body, :size => '40x5' %>
  </div>

  <%=f.hidden_field :user_id%>

  <div class="actions">
    <%= f.submit "Add comment", :name => 'public_comment'%>
  </div>
  
  
<% end %>
